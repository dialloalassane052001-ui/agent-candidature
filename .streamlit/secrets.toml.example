# Modele de secrets pour le deploiement (Streamlit Community Cloud).
#
# NE PAS renommer ce fichier en secrets.toml avec ta vraie cle sur GitHub.
# En local : utilise plutot le fichier .env a la racine du projet.
# En deploiement : copie-colle ce contenu (avec ta vraie cle) dans
#   Streamlit Cloud -> Settings -> Secrets.

GEMINI_API_KEY = "colle-ta-cle-gemini-ici"
GEMINI_MODELE = "gemini-2.5-flash"
